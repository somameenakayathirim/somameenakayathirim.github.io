/**
 * PostCSS Configuration
 * 
 * Configures CSS processing plugins:
 * - TailwindCSS: Utility-first CSS framework
 * - Autoprefixer: Automatically adds vendor prefixes for cross-browser compatibility
 */
module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}
